**IMPORTANT: Do not remove content from `CONSTRAINTS.md` without approval.**

`${PROJECT_ROOT}/content/pipeline` is a separate public Python project. `${PROJECT_ROOT}/content/articles` is a separate private article repository. Both are loaded as read-only submodules, while the rest of the project is written in TypeScript. **CONSIDER `content` FOLDER READ-ONLY**
