# Make dark mode the default

The bibliography page is text-heavy and the original white surface was fatiguing, so the app defaults to daisyUI dark semantic colors while retaining a persisted light-mode switcher. The theme is applied from local storage before hydration to prevent a disruptive color flash.
